# 项目状态与进度记录

记录时间：2026-05-03

## 当前代码基准

- 远端仓库：`git@github.com:1126misakp/Dice_Legend-webgame.git`
- 当前分支：`main`
- 当前基线基于 `origin/main`，本轮准备将二次开发收口成果提交并推送。
- 项目技术栈：React 19、TypeScript、Vite、Three.js、cannon-es、Cloudflare Workers、Wrangler。

## 本轮基线收口已完成

### 架构拆分与安全收口

- 新增 `utils/promptTemplates.ts`：
  - 集中管理角色文案、立绘、Live 动态化和本地兜底 prompt。
  - 清理立绘和动态化 prompt 中不符合全年龄幻想战棋定位的描述。
- 新增 `utils/runningHubResult.ts`：
  - 统一解析 RunningHub `taskId`、图片 URL、视频 URL。
  - 统一识别运行中、成功、失败和审核失败信息。
- 新增 `utils/logger.ts`：
  - 开发环境输出详细日志。
  - 生产环境抑制调试和普通信息日志。
- 更新 `utils/apiClient.ts`：
  - 代理请求支持可选 `timeoutMs`。
  - 默认 30 秒超时。
  - 超时统一抛出 `ApiClientError`，错误码为 `REQUEST_TIMEOUT`。
- 更新 `utils/runningHubQueue.ts`：
  - 队列调试输出改为使用统一 logger。

### 大组件减负

- `App.tsx`：
  - 移除组件内大段角色文案 prompt 和立绘 prompt。
  - 移除局部 `fetchWithTimeout`、`TimeoutError`、`ApiError`。
  - 静态立绘 RunningHub 解析改用 `runningHubResult`。
  - 引入 `ThreeRarityAura` 用于结果区域稀有度光环表现。
- `components/CharacterCard.tsx`：
  - Live 动态化 prompt 改用 `promptTemplates`。
  - Live 动态化 RunningHub 解析改用 `runningHubResult`。
  - 主要语音和 Live 日志改用 `logger`。

### UI 与视觉升级

- 新增 `components/ThreeRarityAura.tsx`：
  - 使用 Three.js 渲染稀有度粒子、圆环和光晕。
  - 支持 `normal`、`large`、`burst` 三种强度。
- 更新 `components/DiceCanvas.tsx`：
  - 画布改为透明背景。
  - 调整光照和召唤台圆环视觉。
- 更新 `components/ApiSettingsPanel.tsx`：
  - 改为羊皮纸/幻想面板风格。
  - 优化窄屏布局和能力状态展示。
- 更新 `components/InventoryBar.tsx`、`components/ResultPanel.tsx`、`components/CharacterCard.tsx`：
  - 统一暗色幻想和羊皮纸视觉语言。
  - 优化移动端换行、最小宽度和可读性。
- 新增项目内视觉资产：
  - `public/ui/academy-hall.png`
  - `public/ui/arcane-metal-ui.png`
  - `public/ui/astrolabe-crest.png`
  - `public/ui/parchment-panel.png`
- 更新 `index.html`：
  - 页面标题改为「纹章传说」。
  - 页面底色切换为深色背景。

## 前序已完成能力

- BYOK API Key 模式：
  - `utils/apiKeyStore.ts` 管理 OpenRouter、RunningHub、MiMo Key、OpenRouter 模型名和文案供应商。
  - `components/ApiSettingsPanel.tsx` 支持用户填写、保存和清除本地 Key。
  - 前端不再读取 `VITE_*` 密钥。
- Cloudflare Worker 代理：
  - `src/worker/index.ts` 实现 OpenRouter、RunningHub、MiMo 白名单代理。
  - `wrangler.jsonc` 配置单 Worker 托管静态资源和 `/api/*`。
  - Worker 不保存平台密钥，只读取本次请求的用户 Key。
- 前端降级流程：
  - 缺当前文案供应商 Key 时使用本地角色文案、立绘 prompt、Live prompt 和语音台词兜底。
  - 缺 RunningHub 时跳过立绘和动态化，显示文字角色卡/占位视觉。
  - 缺 MiMo 时跳过语音生成，语音槽位显示缺失态。

## 当前验证结果

已通过：

- `npm run typecheck`
- `npm run build`
- `npx wrangler deploy --dry-run`
- `npm audit --audit-level=moderate`

安全搜索结果：

- 源码和构建产物中未发现旧前端密钥环境变量名：
  - `OPENROUTER_API_KEY`
  - `RUNNINGHUB_API_KEY`
  - `MIMO_API_KEY`
  - `GEMINI_API_KEY`
  - `VITE_`
- 源码和构建产物中未发现本轮安全搜索指定的不适合全年龄 prompt 残留词：
  - `裙底`
  - `乳沟`
  - `Seductive`
  - `性感`
  - `性暗示`
  - `裸露`
  - `挑逗`

## 当前已知情况

- `npm run build` 仍提示主 chunk 超过 500 KB；当前主包约 934 KB，主要来自 Three.js、React 和仍集中的业务组件。
- `index.html` 仍引用 `/index.css`，构建时提示该文件不存在并保留运行时解析；这是原项目既有行为，目前不阻塞。
- `App.tsx` 和 `CharacterCard.tsx` 已缩短，但仍超过 1200 行，后续仍需继续拆 hook/service。
- 下载逻辑和 `voiceService` 中仍有部分 `console.log`，日志治理尚未完全完成。
- RunningHub 轮询仍由前端驱动，尚未实现统一取消机制。
- 本轮未使用真实 OpenRouter、RunningHub、MiMo Key 做端到端手动验证。

## 清理记录

- 已清理：
  - `public/._ui`，macOS 资源叉文件，不属于项目资产，不应提交。
- 保留：
  - `dist/`，由构建和 Wrangler dry-run 产生，保留在本地用于验证，仍被 Git 忽略。
  - `public/ui/*.png`，本轮新增项目视觉资产，属于需要提交的源码资产。

## 下一步建议

1. 拆出 `hooks/useContractGeneration.ts` 和 `services/contractService.ts`，继续降低 `App.tsx` 复杂度。
2. 拆出 `hooks/useLiveGeneration.ts`、`utils/mediaDownload.ts` 和 `components/VoiceSlots.tsx`，继续降低 `CharacterCard.tsx` 复杂度。
3. 给 RunningHub 轮询增加统一取消机制，避免页面切换后继续更新旧状态。
4. 对 `voiceService` 和下载逻辑继续接入 `logger`。
5. 做构建分包，优先拆 Three.js 相关代码，降低主 chunk 体积。
6. 使用真实三方 Key 手动验证完整契约、立绘、语音和 Live 动态化流程。

## 2026-05-03 二次开发推进：架构拆分优先

本轮已完成：

- 新增 `utils/asyncControl.ts`，提供可取消延迟、取消错误和取消识别工具。
- 更新 `utils/apiClient.ts`，支持外部 `AbortSignal`，并区分 `REQUEST_TIMEOUT` 与 `REQUEST_CANCELLED`。
- 更新 `utils/runningHubQueue.ts`，等待中的 RunningHub 队列任务可随 `AbortSignal` 取消；执行中任务由调用方取消请求和轮询。
- 新增 `utils/runningHubTask.ts`，统一封装 RunningHub 提交、轮询、失败状态识别、最终查询和结果提取。
- 新增 `services/contractService.ts` 与 `hooks/useContractGeneration.ts`，将契约生成、立绘 prompt、静态立绘、语音并发和进度状态从 `App.tsx` 拆出。
- 新增 `hooks/useLiveGeneration.ts`，将 Live 动态化提示词、队列、RunningHub 轮询、取消和视频状态从 `CharacterCard.tsx` 拆出。
- 新增 `utils/mediaDownload.ts` 与 `components/VoiceSlots.tsx`，拆出下载逻辑和语音槽位 UI。
- `services/voiceService.ts` 与 `services/audioService.ts` 剩余裸日志改用统一 `logger`；下载日志也改用 `logger`。
- 移除 `index.html` 中不存在的 `/index.css` 引用。
- `vite.config.ts` 增加 `manualChunks`，拆出 `react`、`three`、`physics` chunk。

当前体量变化：

- `App.tsx`：约 1284 行降至 979 行。
- `components/CharacterCard.tsx`：约 1296 行降至 709 行。
- 主业务 chunk：约 934 KB 降至约 344 KB；构建不再出现 500 KB chunk 警告。

本轮验证结果：

- `npm run typecheck` 通过。
- `npm run build` 通过。
- `npx wrangler deploy --dry-run` 通过。
- `npm audit --audit-level=moderate` 通过，0 个漏洞。
- 旧密钥变量和指定不适合全年龄 prompt 词搜索无命中。

清理记录：

- 未新增临时测试脚本或临时目录。
- 保留 `dist/` 与 `.wrangler/tmp`，均为项目既有忽略产物，分别来自生产构建和 Wrangler dry-run。

## 2026-05-03 二次开发推进：移动端、Schema 与角色卡拆分

本轮已完成：

- 真实三方 Key 验收继续搁置，未在仓库、终端或文档中接触真实 Key。
- Worker payload schema 深校验：
  - OpenRouter 校验 `model`、`messages` 与多模态 `content`。
  - RunningHub run 校验 `webappId` 与 `nodeInfoList`。
  - RunningHub outputs 校验 `taskId`。
  - MiMo TTS 校验 `model`、`messages`、`audio.format`、`stream`。
  - 校验失败统一返回 `INVALID_PAYLOAD` / HTTP 400，且不触发上游请求。
- Worker 测试从 10 条扩展到 16 条，新增缺必填字段、类型错误不上游、多模态合法转发等场景。
- `components/CharacterCard.tsx` 继续拆分：
  - 主文件从约 715 行降至 189 行。
  - 新增 `components/character-card/` 子模块，拆出稀有度特效、媒体层、顶部控制区、底部信息区、Live 确认弹窗、原图工具条、职业/种族/属性样式映射。
- 移动端 UI 小修：
  - 顶部工具区缩小移动端间距并限制最大宽度。
  - 风格输入面板在窄屏下改为可换行宽度。
  - 右下隐藏界面按钮在移动端上移，避免压住底部主控区。
  - 底部主控区与奖励选择按钮减少移动端 padding 和最小列宽。
  - 角色卡使用 `100dvh` 与 `100vw` 约束卡面宽度。
  - 语音槽位在小于 390px 宽度时轻微缩放，减少溢出风险。

移动端检查结果：

- 使用本地 Vite 页面 `http://127.0.0.1:3000/` 做浏览器检查。
- DOM 快照确认主界面顶部工具、底部主控、API 缺失提示、API 设置弹窗可见且可打开/关闭。
- 浏览器截图接口在当前页面上超时，未能保存截图；本轮按 DOM 快照和代码级窄屏约束完成检查。
- 未执行真实手机和真实 Key 链路。

本轮验证结果：

- `npm run ci` 通过。
- CI 内部完成：
  - `npm run typecheck` 通过。
  - `npm run test:worker` 通过，16 条 Worker 测试全部通过。
  - `npm run build` 通过，主业务 chunk 约 345 KB。
  - `npm run cf:dry-run` 通过。
  - `npm run audit` 通过，0 个漏洞。

清理记录：

- 已清理本轮编辑产生的未跟踪 macOS `._*` 资源叉文件。
- 未新增一次性临时测试脚本或临时目录。
- 保留 `src/worker/index.test.ts` 与 `components/character-card/`，属于项目测试和源码模块。
- 保留 `dist/` 与 `.wrangler/`，均为项目既有忽略产物，分别来自生产构建和 Wrangler dry-run / 本地 Worker 缓存。

## 2026-05-03 二次开发推进：验收护栏收口

本轮已完成：

- `src/worker/index.ts` 增加结构化请求错误：
  - 非法 JSON 返回 `INVALID_JSON` / HTTP 400。
  - 无 `Content-Length` 但实际请求体超限返回 `PAYLOAD_TOO_LARGE` / HTTP 413。
  - 保持代理错误响应壳 `{ ok: false, error: { code, message } }` 不变。
- 新增 `src/worker/index.test.ts`，使用 Node 内置 `node:test` 直接测试 Worker fetch 入口，并 mock 上游第三方请求。
- 新增 `test:worker`、`cf:dry-run`、`audit`、`ci` 脚本。
- 新增 `.github/workflows/ci.yml`：
  - Node 22。
  - `npm ci`。
  - `npm run typecheck`。
  - `npm run test:worker`。
  - `npm run build`。
  - `npm run cf:dry-run`。
  - `npm run audit`。
- 新增轻量 dev 依赖 `tsx`，用于运行 TypeScript Worker 测试。

Worker 测试覆盖：

- 未知 `/api/*` 返回 `NOT_FOUND`。
- 非 POST 返回 `METHOD_NOT_ALLOWED`。
- 非 JSON 返回 `UNSUPPORTED_MEDIA_TYPE`。
- 非法 JSON 返回 `INVALID_JSON`。
- 无 Key 返回 `MISSING_API_KEY`。
- 无 `Content-Length` 但实际超大 body 返回 `PAYLOAD_TOO_LARGE`。
- OpenRouter 代理只转发白名单字段和用户 Key。
- RunningHub run 代理把用户 Key 放入上游请求体。
- 上游 JSON 错误和文本错误均归一为 `UPSTREAM_ERROR`。

本轮验证结果：

- `npm run ci` 通过。
- CI 内部完成：
  - `npm run typecheck` 通过。
  - `npm run test:worker` 通过，10 条 Worker 测试全部通过。
  - `npm run build` 通过，主业务 chunk 约 344 KB。
  - `npm run cf:dry-run` 通过。
  - `npm run audit` 通过，0 个漏洞。

真实 Key 端到端验收状态：

- 本轮未在仓库、终端或文档中接触真实 OpenRouter、RunningHub、MiMo Key。
- 真实 Key 完整链路仍待用户在浏览器 API 设置面板手动填写后验收：
  - OpenRouter 角色文案与提示词。
  - RunningHub 静态立绘。
  - MiMo `MiMo-V2.5-TTS-VoiceDesign` 语音。
  - 角色卡出场语音和语音槽位播放。
  - RunningHub Live 动态化。
  - 图片和视频下载。

清理记录：

- 已清理所有未跟踪的 macOS `._*` 资源叉文件，范围包括根目录、`components/`、`hooks/`、`services/`、`utils/`、`src/`、`docs/`、`step-repo/`、`.github/` 与 `.wrangler/` 下的资源叉文件。
- 未新增一次性临时测试脚本或临时目录；`src/worker/index.test.ts` 是项目测试文件，需保留并提交。
- 保留 `dist/` 与 `.wrangler/`，均为项目既有忽略产物，分别来自生产构建和 Wrangler dry-run / 本地 Worker 缓存。

## 2026-05-03 二次开发推进：类型收口与 UI 小组件拆分

本轮已完成：

- 新增 `components/ApiErrorModal.tsx`，将 API 错误弹窗从 `App.tsx` 拆出。
- 新增 `RawDiceResult` 类型，并让 `App.tsx` 与 `components/DiceCanvas.tsx` 共享骰子原始结果契约。
- `components/CharacterCard.tsx` 的职业样式改为判别联合类型，移除彩虹职业样式的 `as any`。
- `utils/apiClient.ts` 为 OpenRouter、RunningHub、MiMo 代理响应补充基础返回类型。
- `utils/runningHubResult.ts` 与 `utils/runningHubTask.ts` 改为 `unknown` + 类型守卫解析外部 API 返回，降低 RunningHub 响应结构漂移带来的维护风险。
- `hooks/useContractGeneration.ts`、`hooks/useLiveGeneration.ts`、`services/voiceService.ts` 收紧错误和 JSON 解析类型。
- `services/audioService.ts` 补充 Safari `webkitAudioContext` 类型，不再使用 `window as any`。
- `utils/logger.ts` 改用 `vite/client` 类型支持的 `import.meta.env.DEV`。

当前体量变化：

- `App.tsx`：约 979 行降至 958 行。
- `components/ApiErrorModal.tsx`：新增 39 行。
- `components/CharacterCard.tsx`：因补充职业样式类型约 709 行增至 715 行。
- 前端源码 `any/as any` 搜索已无命中；仅 Worker 保留一处上游失败诊断 `console.warn`，只记录接口路径、HTTP 状态和耗时，不记录密钥或响应体。

本轮验证结果：

- `npm run typecheck` 通过。
- `npm run build` 通过，主业务 chunk 约 344 KB，未出现 500 KB chunk 警告。
- `npx wrangler deploy --dry-run` 通过。

清理记录：

- 未新增临时测试脚本或临时目录。
- 保留 `dist/` 与 `.wrangler/tmp`，均为项目既有忽略产物，分别来自生产构建和 Wrangler dry-run。

## 2026-05-06 二次开发阶段性收口：Cloudflare 上线与 UI 修复

本阶段已完成：

- Cloudflare 自动部署链路已跑通：
  - 使用 `.github/workflows/deploy.yml` 自动部署到 Cloudflare Worker。
  - GitHub 仓库 Secrets 已配置 `CLOUDFLARE_API_TOKEN` 与 `CLOUDFLARE_ACCOUNT_ID`。
  - 本机 Wrangler 仍未登录，但不阻塞 GitHub Actions 部署。
- 线上主页：
  - `https://dice-legend-webgame.roysindywang.workers.dev`
- 角色卡视觉修复：
  - 移除覆盖立绘的全卡纸纹和星盘装饰层，避免遮住真实立绘。
  - SSR/UR 扫光层降低强度并加遮罩，避免大面积洗白画面。
  - 卡面信息块恢复毛玻璃层次，减少硬描边。
  - 修复语音技能槽上下被裁切的问题，语音标签不再使用负向贴边定位。
- 主界面布局调整：
  - 底部主控区拆掉横向大框。
  - 底部仅保留居中的召唤/契约主按钮区域。
  - 刻印、灌铅骰子、Crest/Weighted 状态和 API 未配置提示迁移到左上「骰子传说」下方，并改为竖版排布。
  - `InventoryBar` 支持 `horizontal` 与 `vertical` 两种方向。
- MiMo 代理迁移：
  - Worker 文案与 TTS 上游为 `https://token-plan-cn.xiaomimimo.com/v1/chat/completions`。
  - 使用请求头 `api-key` 转发用户 MiMo Key。
  - 文案默认使用 `MiMo-V2.5-Pro`，语音使用 `MiMo-V2.5-TTS-VoiceDesign`。
  - 请求体白名单覆盖文案 `model/messages/max_completion_tokens` 与 TTS `model/messages/audio/stream`。
  - Worker 测试锁定 MiMo Token Plan 域名、文案 payload 和 VoiceDesign payload。

本阶段关键提交：

- `f7608ca`：修复卡面遮挡与 MiMo 代理。
- `c5ca5dc`：修正 MiMo 域名并优化卡面玻璃层次。
- `5a9399a`：调整主界面布局与语音槽间距。

本阶段验证结果：

- `npm run typecheck` 通过。
- `npm run test:worker` 通过，17 条 Worker 测试全部通过。
- `npm run build` 通过。
- `npm run cf:dry-run` 通过。
- Cloudflare GitHub Actions 部署成功。
- 线上资源已切换到最新构建包：
  - `dist/assets/index-BtR6jDTF.js`。

当前已知情况：

- MiMo 若仍返回鉴权失败，当前优先判断为用户填写的 MiMo Key 与 Xiaomi MiMo API 开放平台或 VoiceDesign 权限不匹配，需要用真实有效 Key 进一步确认。
- RunningHub 线上轮询偶发 `ERR_CONNECTION_CLOSED` / `ERR_SSL_PROTOCOL_ERROR` 仍需结合真实任务与网络环境继续观察。
- 主界面和角色卡 UI 已完成一轮修复，但仍建议后续做浏览器截图级回归，覆盖桌面、窄屏和不同稀有度卡面。
- GitHub Actions 提示 `actions/checkout@v4`、`actions/setup-node@v4` 运行时 Node.js 20 将在 2026-06-02 后默认切换到 Node.js 24，后续可评估显式环境变量或升级 action。

清理记录：

- 已清理 `dist/`，生产构建产物由部署流程重新生成。
- 已清理 `.wrangler/`，Wrangler 本地缓存不保留。
- 已清理 `._*`，macOS 资源叉文件不提交。
- 保留 `node_modules/`，属于本地开发依赖环境。
